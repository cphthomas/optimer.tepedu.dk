[build]
  command = "npm install && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "16.20.2"
  NPM_VERSION = "7.24.2"
  CI = "false"
  NPM_FLAGS = "--legacy-peer-deps"

[build.processing]
  skip_processing = true 